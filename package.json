{
  "name": "shifra-virtual-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "start": "node server.mjs", "dev": "node --watch server.mjs", "check": "node --check script.js && node --check server.mjs && node --check api/chat.js && node --check service-worker.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.webmanifest', 'utf8'))\"" },
  "dependencies": { "dotenv": "^16.6.1", "express": "^5.1.0", "groq-sdk": "^0.33.0" }
}
